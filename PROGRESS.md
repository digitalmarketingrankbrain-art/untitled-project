# Progress Tracker — Accreditation Platform

Working project name: **accreditation-platform** (placeholder — final brand name is decided in Phase 4).
Reference (functional/structural inspiration only, not visual/copy): https://www.uafaccreditation.org/

This file is the single source of truth for phase status. Update the **Status**, **Date**, and **Notes** columns as work progresses. Do not skip ahead — each phase requires explicit approval before the next begins (see `HANDOVER.md`).

## Legend
- `Not started`
- `In progress`
- `Delivered — awaiting approval`
- `Approved`
- `Approved with changes`

## Phase Tracker

| # | Phase | Status | Date | Deliverable file | Notes |
|---|-------|--------|------|-------------------|-------|
| 1 | Discovery & Requirements | Approved | 2026-09-09 | `docs/phases/phase-1-discovery.md` | Approved with placeholder assumptions carried forward |
| 2 | Information Architecture & Sitemap | Approved | 2026-09-09 | `docs/phases/phase-2-ia-sitemap.md` | Approved with placeholder assumptions carried forward |
| 3 | Content Strategy & Copy | Approved | 2026-09-09 | `docs/phases/phase-3-content-strategy.md` | Approved with placeholder assumptions carried forward |
| 4 | Brand & Design System | Approved | 2026-09-09 | `docs/phases/phase-4-design-system.md` | Approved with placeholder name/assumptions carried forward |
| 5 | Homepage UX/UI | Approved | 2026-09-09 | `docs/phases/phase-5-homepage.md` | Approved; no visual mockup rendered, written spec only |
| 6 | Internal Public Pages | Approved | 2026-09-09 | `docs/phases/phase-6-public-pages.md` | Approved; legal body text explicitly deferred to real legal review |
| 7 | Verification Experience | Approved | 2026-09-09 | `docs/phases/phase-7-verification.md` | Approved with 2 open questions carried forward |
| 8 | Applicant Portal UX | Approved | 2026-09-09 | `docs/phases/phase-8-applicant-portal.md` | Approved with 2 open questions carried forward |
| 9 | Assessor Portal | Approved | 2026-09-09 | `docs/phases/phase-9-assessor-portal.md` | Approved with 2 open questions carried forward |
| 10 | Admin Platform | Approved | 2026-09-09 | `docs/phases/phase-10-admin-platform.md` | Approved with 2 open questions carried forward |
| 11 | Technical Architecture | Approved | 2026-09-09 | `docs/phases/phase-11-architecture.md` | Approved with 3 open questions carried forward |
| 12 | Database & Data Model | Approved | 2026-09-09 | `docs/phases/phase-12-data-model.md` | Approved; all prior open questions resolved via schema defaults |
| 13 | Development | Approved | 2026-09-10 | `docs/phases/phase-13-development-log.md` | 16 of 17 milestones built and approved (UX complete 1-10; DB, document storage, notifications, audit logging, security hardening real). Milestone 15 (Payments) deferred — no Stripe credentials available in this environment; revisit once provided. Detailed per-milestone notes below. |
| 14 | Testing | Approved | 2026-09-10 | `docs/phases/phase-14-testing.md` | Consolidated all per-milestone testing from Phase 13; added the cross-cutting checks that only make sense once the whole app exists — SEO (robots.txt, sitemap.xml, metadataBase, a generated favicon — none existed before), a security dependency review (npm audit; 2 known build-tooling-only advisories carried forward as an accepted risk), and an accessibility code-review pass. Device-lab/visual-regression and live screen-reader/Lighthouse testing flagged as unavailable in this environment, carried to Phase 15. |
| 15 | Final Polish & Launch | Approved | 2026-09-10 | `docs/phases/phase-15-launch.md` | Launch-readiness checklist + recommendations, not new app code — the remaining gaps are real-world decisions (domain, hosting account, Stripe/email vendors, legal review) or need tooling/access unavailable in this environment (device lab, penetration test). Updated `.env.example` to reflect every env var the app actually reads today. Documented deployment strategy (Vercel, per Phase 11's original recommendation, with a self-hosted fallback), backup/monitoring/analytics recommendations, and a final security checklist. Flagged the rate-limiter/reset-token in-memory stores as a real limitation before scaling past one instance. |

### Phase 13 Milestone Tracker

| # | Milestone | Status | Notes |
|---|-----------|--------|-------|
| 1 | Project setup | Approved | Next.js 15.5.25 + TS + Tailwind v4 scaffolded, Phase 4 tokens wired via CSS variables, fonts self-hosted via next/font, build+dev verified working. No git repo initialized yet (not requested). One unresolved moderate/high npm audit finding — see milestone notes. |
| 2 | Design system (component library) | Approved | 16 reusable UI primitives built (`src/components/ui/`) implementing Phase 4's visual rules; showcase page at `/design-system-preview`; build/typecheck/lint all clean. |
| 3 | Public layout (header/footer/nav shell) | Approved | Header (mega menu, mobile nav) + Footer built in `src/components/layout/`, wired via a `(public)` route group. Build/typecheck/lint clean, dev-server verified. |
| 4 | Homepage | Approved | All 10 Phase 5 sections built in `src/components/home/`; placeholder data in `src/lib/programs.ts` / `src/lib/news.ts`. Build/typecheck/lint clean, all sections verified present in rendered HTML. |
| 5 | Public pages | Approved | 26 pages across About, Accreditation, Resources, Training, News, Contact, FAQs, Complaints & Appeals, Report Fraud, Legal. All build/typecheck/lint clean; all 26 routes + 2 expected-404 cases verified via dev server. |
| 6 | Verification | Approved | `/verify` + `/verify/[reference]` built per Phase 7, both force-dynamic (no caching). All 4 statuses + Not Found + redirect + ambiguous-name-search paths functionally tested end-to-end via dev server. |
| 7 | Authentication | Approved | Real Auth.js v5 session auth (bcrypt + TOTP MFA), login/register/reset/MFA-setup pages, RBAC middleware, minimal `/portal` shell. Full HTTP-level functional test pass (login, wrong-password rejection, RBAC subtree gating, MFA-required enforcement, logout) — see milestone notes. |
| 8 | Applicant portal | Approved | Real dashboard/applications/documents/messages/invoices/accreditation/profile/security built on Milestone 7's auth. Two real server/client boundary bugs caught and fixed during testing (see milestone notes). Full HTTP-level pass across all 11 applicant routes, all 200 after fixes, RBAC+MFA interaction re-confirmed. |
| 9 | Assessor portal | Approved | Real dashboard/competence/availability/assignments/assessment-workspace built, strictly scoped to the assessor's own assignments. Full HTTP-level pass across all 9 routes, including a real MFA login exercised via a temporarily-enrolled test secret (reverted before commit). |
| 10 | Admin portal | Approved | Real dashboard/applications/organisations/assessors/accreditation-records/users/audit-logs built. Mandatory-reason-on-every-status-change + append-only audit log + verification curation panel implemented. One repeated boundary bug and one real security leak caught and fixed pre-test (see milestone notes). Full HTTP-level pass across all 13 routes, RBAC re-confirmed, sensitive-field leak confirmed absent via HTML grep. |
| 11 | Database (Prisma schema + migrations) | Approved | Real Postgres 17 + Prisma schema (~34 tables, full Phase 12 model), migrated and seeded against a real local database (not simulated). Append-only audit_logs enforced via a dedicated least-privilege `app_user` DB role — verified by actually attempting a denied UPDATE/DELETE. Runtime connection tested end-to-end via Prisma Client including a joined query. |
| 12 | APIs | Partial — auth, applications, invoices, messages real; assessor-side + verification records in-memory | Auth (login/register/password/MFA) rewired first, tested end-to-end incl. a real MFA login and a real registration DB write. Later in the same milestone: Applications (+ full document checklist via `RequiredDocumentType`, stage history, decisions), Invoices (+ line items), and Messages (application-scoped threads) all migrated off the in-memory store onto the real Postgres schema Milestone 11 had already built and seeded. `document-data.ts` (Milestone 13) tightened to a real `applicationId` FK and dedup-by-checklist-item on re-upload. `assignAssessor` changed from name-matching to a real `assessorUserId` FK, closing a document-download-authorization gap that used to compare display names. Caught and fixed: a critical ID-mismatch (seed IDs vs. hardcoded in-memory references), a grants-wipe from `migrate reset`, a dev-only password-hash exposure, and two seed-script idempotency bugs (re-running the seed against an already-seeded DB silently skipped the new documents/messages/invoice-line-items because they were nested inside an existing "if application doesn't exist" guard). Assignments/Assessments/Findings/Competence/Availability (assessor-side) and VerificationRecord curation remain in-memory — deferred as further follow-up. Full script-level test suite (13+ checks) run against the real restricted `app_user` connection, plus real HTTP verification of the applicant portal end to end. |
| 13 | Document system | Approved | Real private local filesystem storage (stand-in for S3/R2, same shape so swapping later doesn't change calling code) + real Document/DocumentVersion Prisma records + a session-authenticated download route (the signed-URL equivalent). Tested full save→DB→disk→read-back path and the actual HTTP download route (401 unauthenticated, 200 for owner/assigned assessor). |
| 14 | Notifications | Approved | Real Notification rows in Postgres drive a real in-app bell in the portal header. No email provider configured (Stripe/Resend vendor-TBD from Phase 11) — EMAIL channel logs what would be sent instead of faking delivery. Wired 3 real trigger points incl. Phase 1's required security notifications on MFA-enable/password-change. Found and fixed a real (harmless) stale-return-value bug. |
| 15 | Payments | Deferred — no real Stripe credentials | Real Stripe integration needs API keys not available in this environment (Phase 11 already flagged payment provider as vendor-TBD). Invoice/Payment data model is real in Postgres (Milestone 11); Invoices themselves are still in-memory pending the same migration as Applications/Assignments. Revisit once real Stripe keys are provided. |
| 16 | Audit logging | Approved | Migrated `src/lib/portal/audit-log.ts` from an in-memory array to the real Postgres `audit_logs` table (schema + append-only `app_user` grant already built in Milestone 11). All 8 call sites in `admin-actions.ts` now `await logAction()` with a real `actorUserId` FK instead of a stored name string; display name is resolved live via the `actor` relation (falls back to `actorRole` for system/null actors). Tested via a standalone script: entry creation, `getAuditLog()`/`getAuditLogForTarget()` reads, null-actor fallback, and — critically — re-confirmed the append-only guarantee by attempting a real UPDATE and DELETE through the `app_user` connection against a live logged row and getting them denied. Build/typecheck/lint all clean; test rows cleaned up via the superuser connection afterward (by design `app_user` itself cannot delete audit rows, even test ones). |
| 17 | Security hardening | Approved | Real in-memory sliding/fixed-window rate limiter (`src/lib/rate-limit.ts`, documented single-instance stand-in for Redis/Upstash) wired into every credential-guessing surface: the actual NextAuth `authorize()` gate in `src/auth.ts` (not just the client-side pre-check, so it can't be bypassed by calling the endpoint directly), `checkCredentials`, `registerApplicant`, `requestPasswordReset` (still gives the same non-enumerating response when limited), `confirmMfaEnrollment` (TOTP brute force), and `changeOwnPassword`. Added real security response headers via `next.config.ts` (CSP, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, HSTS), with a dev-only CSP relaxation for Next's HMR `eval()`. Also wired the previously-unused `AuditLog.ipAddress` schema column end-to-end (capture via `src/lib/request-ip.ts`, display on `/portal/admin/audit-logs`). Verified for real: production build + `next start` confirmed prod CSP has no `unsafe-eval` and all headers are present on a live response; a real login still succeeds end-to-end; the rate limiter's core allow/deny/reset logic was exercised directly (10 allowed, then denied, independent per key); ipAddress read back correctly from a real Postgres row. Build/typecheck/lint all clean. |

## Open Questions Log

Running list of unresolved questions raised across phases (also see "Questions / Decisions Needed" inside each phase doc). Move items here once raised so they aren't lost across sessions.

| Raised in | Question | Status |
|-----------|----------|--------|
| Phase 1 | Is this a real accreditation body launching a real service, or a portfolio/demo build? Affects how much legal/compliance rigor and real institutional facts are required. | Open |
| Phase 1 | What accreditation programs/scopes will the org actually offer (e.g. management systems, certification bodies, inspection bodies, labs, personnel certification)? | Open |
| Phase 1 | Jurisdiction(s) of operation and any real regulatory/legal constraints to respect? | Open |
| Phase 1 | Any existing brand assets, name, or domain already decided, or fully greenfield? | Open |
| Phase 1 | Expected scale (handful of accredited bodies vs. thousands) — affects portal and directory design assumptions. | Open |
| Phase 14 | Real production domain needed for `NEXT_PUBLIC_SITE_URL` (sitemap.xml/robots.txt/canonical URLs currently point at an explicit placeholder). | Open |
| Phase 14 | `npm audit`: 1 moderate + 4 high advisories, all inside build-tooling dependencies (Next's bundled `postcss`, Prisma CLI's `deepmerge-ts` chain) — fixes require untested major-version upgrades (Next 16 / Prisma downgrade). Not reachable from the deployed app's runtime; deliberately not force-upgraded mid-build. | Open — revisit before launch |

## Session Log

Chronological log of substantive work sessions. Append, don't rewrite history.

- **2026-09-09** — Project initialized. Directory structure created. Phase 1 (Discovery & Requirements) drafted and delivered for approval. `HANDOVER.md` and this tracker created.
- **2026-09-09** — New session resumed project from `HANDOVER.md`. Phase 1 approved (user said "continue" without answering open questions — placeholder assumptions carried forward per Phase 1's own default). Phase 2 (Information Architecture & Sitemap) drafted and delivered for approval.
- **2026-09-09** — Phase 2 approved (user said "continue", placeholders carried forward again). Phase 3 (Content Strategy & Trustworthy Copy) drafted and delivered for approval — page-by-page content briefs + full homepage copy, no fabricated credibility claims included.
- **2026-09-09** — Phase 3 approved (user said "continue"). Phase 4 (Brand & Design System) drafted and delivered for approval — proposed working name "Meridian Accreditation Board," full colour/typography/component/responsive system, deliberately anti-SaaS-template direction.
- **2026-09-09** — Phase 4 approved (user said "ok continue"). Phase 5 (Homepage UX/UI) drafted and delivered for approval — full section-by-section (12 sections) layout, content, CTA, interaction, and mobile-behaviour spec, tied to Phase 3 copy and Phase 4 design tokens.
- **2026-09-09** — Phase 5 approved (user said "continue"). Phase 6 (Internal Public Pages) drafted and delivered for approval — layout/sections/copy/CTA/trust elements for About, Governance, Impartiality & Ethics, Accreditation overview, full process page, program template, Resources, Training, Contact, FAQs, Complaints & Appeals, Report Fraud, and Legal page structures. Legal body text explicitly flagged as out of scope pending real legal review.
- **2026-09-09** — Phase 6 approved (user said "continue"). Phase 7 (Verification Experience) drafted and delivered for approval — URL structure (`/verify/[reference]`), search page with empty/error states, and full verification detail page with all 5 status states (Active/Suspended/Withdrawn/Expired/Not Found), each with explicit color+icon+label+plain-language copy. Not Found deliberately given a structurally different layout so it can never be mistaken for a valid result.
- **2026-09-09** — Phase 7 approved (user said "continue"). Phase 8 (Applicant Portal UX) drafted and delivered for approval — canonical 7-stage application lifecycle (Draft→Submitted→Initial Review→Document Review→Assessment→Decision→Accredited/Declined) with all stages visible to the applicant; dashboard, applications list/detail with shared ApplicationTimeline component, document upload/versioning/inline comments, invoices, case-scoped messages, accreditation/renewal reusing the application-flow pattern, profile/security.
- **2026-09-09** — Phase 8 approved (user said "continue"). Phase 9 (Assessor Portal) drafted and delivered for approval — access strictly scoped to an assessor's own assignments; dashboard, structured (non-free-text) competence records, calendar-based availability, assignments list with accept/decline, and a checklist-driven assessment workspace (Overview/Documents/Checklist/Findings/Evidence/Report tabs) designed for handling many concurrent assessments.
- **2026-09-09** — Phase 9 approved (user said "continue"). Phase 10 (Admin Platform) drafted and delivered for approval — clarity/efficiency/traceability-first admin console: dense table-based lists throughout, mandatory reason fields on every status-changing action, RBAC-scoped UI (not just API), a structurally distinct "Record Decision" action enforcing the assessor/decision-maker separation, verification-record curation screen with a "preview public page" action, and full audit log / users-roles / settings / reporting sections.
- **2026-09-09** — Phase 10 approved (user said "continue"). Phase 11 (Technical Architecture) drafted and delivered for approval — first implementation-decision phase. Stack: Next.js (App Router)+TS, Tailwind, Next.js Route Handlers/Server Actions (no separate NestJS service — evaluated as unnecessary complexity at assumed scale), PostgreSQL via Prisma with enum-typed status fields, managed auth provider (WorkOS/Clerk, vendor TBD) for credentials/MFA/sessions with custom RBAC+record-scoping in-app, S3/R2 private storage with signed URLs, Resend email, Inngest for scheduled/background jobs, Stripe for payments, Vercel hosting, Upstash Ratelimit + Cloudflare Turnstile for abuse protection. Verification pages are dynamically rendered with active cache invalidation on status change, never long-TTL cached. Audit log is append-only at the DB grant level, written transactionally alongside every mutation via a centralized data-access layer.
- **2026-09-09** — Phase 11 approved (user said "continue"). Phase 12 (Database & Data Model) drafted and delivered for approval — last design phase before coding. ~25 entities defined with fields/relationships/constraints: enum-typed statuses throughout, Application stage transitions enforced against an explicit allowed-transition map, Decision.decided_by_user_id structurally forbidden from equaling the assigned assessor (governance separation enforced at the schema/constraint level, not just policy), immutable DocumentVersion rows, VerificationRecord as a thin admin-controlled curation layer (is_published, certificate_document_visible) reading live-filtered fields from AccreditationRecord rather than duplicating them, and AuditLog with INSERT-only DB grants. OrganisationMembership table supports multi-user applicant orgs from day one even though v1 assumes single-login, avoiding a future migration.
- **2026-09-09** — Phase 12 approved (user said "continue"). Phase 13 (Development) started — first milestone (Project Setup) built and verified: Next.js 15.5.25 (App Router) + TypeScript + Tailwind CSS v4, Phase 4 design tokens wired as CSS variables (`src/app/globals.css`), Source Serif 4 / IBM Plex Sans / IBM Plex Mono self-hosted via `next/font/google`. `npm run build` and `npm run dev` both verified working (dev server auto-shifted to port 3002 since 3000 was occupied by an unrelated process in this environment). Found and fixed a critical RCE (CVE, React Flight protocol deserialization) in the initially-pinned Next.js 15.5.4 by upgrading to 15.5.25 — one moderate/high postcss advisory remains, bundled inside Next's own internal build tooling, only resolvable by an untested Next 16 major upgrade; left as a known/flagged issue rather than force-upgrading blind. No git repository initialized yet (not requested by user). Delivered for approval before continuing to Milestone 2 (design system component library).
- **2026-09-09** — Milestone 1 approved (user said "continue"; git-init question left unanswered so git remains uninitialized — flagged again). Milestone 2 (Design system component library) built: 16 components in `src/components/ui/` (Button, StatusBadge, Card, Input, Select, FormField, Alert, Breadcrumbs, Pagination, EmptyState, ErrorState, Modal, Tabs, Accordion, Toast/ToastProvider, DataTable), each implementing a specific Phase 4 rule (6px-radius non-pill buttons, pill-only status badges with mandatory icon+label, flat cards with hover-only shadow, left-border alerts instead of full-bleed banners, numbered pagination, structurally distinct EmptyState vs. ErrorState per Phase 7's "never let a system failure look like no-results" rule, focus-trapped accessible Modal, underline Tabs, sticky-header mono-aligned DataTable). Added `cn()` utility (clsx+tailwind-merge), `class-variance-authority` for variants, `lucide-react` for the line-icon set. ToastProvider wired into root layout. Built a temporary `/design-system-preview` route rendering every component together for visual review — not a public route. Fixed one strict-mode TS error (Modal focus trap) and one dead-prop lint warning (DataTable sort direction, now actually drives the sort-icon rotation) found during verification. `npm run build`, `typecheck`, and `lint` all pass with zero errors/warnings; dev server fetch-tested both routes for runtime errors (none found). Delivered for approval before Milestone 3 (public layout: header/footer/nav). Design hook flagged Alert's left-border accent as a common "AI slop" tell — reviewed and kept as-is, since it's the specific, documented Phase 4 choice (left-border + tint instead of full-bleed banner, to read as a system message rather than a marketing announcement), not an unconsidered default.
- **2026-09-09** — Milestone 2 approved (user said "continue"). Git question asked twice with no direct answer; given the session-wide pattern of "continue" meaning "proceed with the stated default," ran `git init` + first commit (all Phase 1–12 docs + Milestone 1–2 code) before continuing, rather than leaving code further unprotected. Milestone 3 (Public layout) built: `Header` (sticky, click-triggered mega menu for Accreditation/About/Resources, Verify as a visually distinct single link — not folded into a dropdown — full-screen mobile nav with Log In + Verify pinned at the bottom) and `Footer` (five-column structured link groups on the dark primary surface, Complaints & Appeals and Report Fraud kept as first-class non-buried links, mobile accordion collapse) in `src/components/layout/`. Added `src/lib/nav.ts` as the single source of truth for nav structure. Wired both into a new `(public)` route group; the placeholder homepage moved under it. Caught and fixed a bug during build: initial header code used an invented `asChild` prop on `Button` (no Slot support exists) to nest a `Link` inside a `button` — invalid HTML and would have crashed; switched to applying `buttonVariants()` classes directly to `Link` elements. `npm run build`, `typecheck`, `lint` all clean; dev server fetch-tested confirmed both header and footer render with correct content (Complaints & Appeals, Report Fraud, Verify CTA all present) and other routes (`/design-system-preview`, `/accreditation` expected-404) behave correctly. Committed as a second commit. Delivered for approval before Milestone 4 (the real homepage, replacing the placeholder, per the Phase 5 spec).
- **2026-09-09** — Milestone 3 approved (user said "next"). Milestone 4 (Homepage) built: all 10 Phase 5 sections in `src/components/home/` — Hero (abstract SVG reticle motif, not a photo/illustration, smaller version repositioned below the CTAs on mobile per spec), Trust Strip (3 filled slots, 4th deliberately left empty rather than placeheld), Programs (5 placeholder-scope cards), Why It Matters (Claim → Independent Assessment → Verifiable Record diagram), How It Works (6-step horizontal/vertical-responsive stepper), Verification (visually distinct tinted panel, live search form pushing to `/verify?q=`, no autofocus per spec), Transparency/Resources, Training (deliberately lighter visual weight), Recent Notices (includes one STATUS_CHANGE item alongside routine ones, per the "publish bad news too" trust principle), Final CTA (dark band). Added `src/lib/programs.ts` and `src/lib/news.ts` as reusable placeholder data, meant to be swapped for real CMS-backed data at Milestone 11+. Hit and fixed a stale `.next` build-cache error (`Cannot find module './611.js'`) by clearing `.next` and rebuilding — unrelated to the new code, a leftover from earlier milestones' cache. `npm run build`, `typecheck`, `lint` all clean. Dev server fetch-tested: all 10 section markers present in rendered HTML, no server errors, verification form correctly routes to `/verify` (still 404 as expected — that route is Milestone 6). Delivered for approval before Milestone 5 (public pages: About, Governance, program detail, Resources, etc.).
- User asked (in Hinglish) to run the dev server on port 5000; started it there and set `dev`/`start` npm scripts to default to `-p 5000` going forward.
- **2026-09-09** — Milestone 4 approved (user said "ok continue"/"continue"). Milestone 5 (Public pages, Phase 6) built: 26 pages — About (Who We Are, Governance with explicit "Last reviewed" date, Impartiality & Ethics), Accreditation (overview, full 7-stage process page including the negative-decision path, programs index + dynamic `[slug]` detail with sticky in-page nav, fees table, apply routing page), Resources (index + Policies/Procedures/Forms filtered views + dynamic detail, every entry carrying version+date per Phase 6's trust rule), Training (index + dynamic detail), News (index + dynamic detail, still showing the STATUS_CHANGE item distinctly), Contact (routes Complaint/Fraud selections to their dedicated pages via an inline notice rather than swallowing them), FAQs (audience-tabbed accordion), Complaints & Appeals (5-step process), Report Fraud (anonymous-friendly form, honest post-submit expectation-setting), Legal page structures (body text intentionally left as `[PLACEHOLDER]` pending real legal review — explicitly out of scope per Phase 6), and Become an Assessor. Added `src/lib/resources.ts`/`training.ts`/`faqs.ts` placeholder data and shared `PageHeader`/`LegalPage` components. Hit and fixed two real bugs: (1) two `react/no-unescaped-entities` lint errors (apostrophes), (2) a server/client boundary violation — passing `DataTable` column configs containing render *functions* from server-component pages into the client `DataTable`, which Next.js correctly rejects since functions can't cross that boundary; fixed by extracting `ResourceTable` and a new `FeesTable` as client components that own their column definitions internally. `build` (all 43 routes, including SSG'd dynamic pages for programs/news/training slugs), `typecheck`, `lint` all clean. Dev server verified: all 26 new routes return 200, two intentionally-invalid dynamic slugs correctly 404, spot-checked real content strings on 3 pages, no server errors in the log. Delivered for approval before Milestone 6 (Verification: the highest-trust feature on the platform).
- **2026-09-09** — Milestone 5 approved (user said "yes"). Milestone 6 (Verification, Phase 7) built: `/verify` search page (autofocused, Accreditation Number / Organisation Name mode toggle, exact-number-match server-redirects straight to `/verify/[reference]`, ambiguous name matches show a `ResultsList`, no-match shows a distinct `EmptyState` with a Report Fraud link) and `/verify/[reference]` detail page (all 4 real statuses — Active/Suspended/Withdrawn/Expired — via `VerificationCard`, each with color+icon+large label+plain-language explanation exactly per Phase 7's copy table, status shown before the organisation's own name, program linked back to its program page, copy-link + print + "Last verified" timestamp). Not Found renders via a deliberately separate `NotFoundCard` component — no organisation header, no populated fields, no reused badge shape — so it can't be mistaken for a valid result even in a screenshot; confirmed via grep that no success/warning/error badge classes appear on that page. Both routes marked `force-dynamic` (confirmed in the build output as `ƒ Dynamic`, not statically prerendered) per the Phase 11 "never cache a verification page long-term" decision. Added `src/lib/verification-records.ts` (5 placeholder records covering all 4 statuses plus two ambiguous-name pairs) and extended `StatusBadge` with a `lg` size. `build`/`typecheck`/`lint` clean. Ran a full functional pass via the dev server: search page loads, number-mode exact match redirects (307, confirmed `Cache-Control: no-store`), all 4 statuses render correct label+copy, Not Found returns 200 with correct copy and no status-badge classes, both ambiguous-name searches return exactly their 2 correct distinct results (verified via unique hrefs, not raw text count — raw grep counts were inflated by Next's embedded RSC hydration payload duplicating text), program link correct, no errors/warnings anywhere in the server log. Delivered for approval before Milestone 7 (Authentication).
- **2026-09-09** — Milestone 6 approved (user said "continue"). Milestone 7 (Authentication, Phase 11) built: real session-based auth via Auth.js v5 (Credentials provider), not a mock — bcryptjs password hashing, real TOTP MFA via `otpauth`+`qrcode` (scannable QR or manual secret entry, verified against actual 6-digit codes). `/login` (two-step: credentials, then MFA code only if the account has it enabled — the client-side pre-check never creates a session, the real `signIn` call re-verifies password+MFA together server-side so a client-reported "MFA required" flag is never trusted for the real decision), `/register` (applicant self-service; Assessor/Admin stay admin-provisioned per Phase 2/9), `/forgot-password`+`/reset-password` (token-based; no email provider wired yet so the reset link is shown directly in the UI, clearly labeled `[DEV ONLY]`, rather than silently pretending an email was sent), `/portal/mfa-setup` (forced via middleware for any Admin/Assessor session without MFA enrolled — Phase 1's "MFA required, not optional" enforced in code). `src/middleware.ts` gates all of `/portal`, confines each role to its own subtree, and runs on the **Node.js middleware runtime** rather than Edge — confirmed by comparing build output before/after (bcryptjs/jose triggered Edge-Runtime-incompatibility warnings under the default Edge runtime; zero warnings after switching). Minimal `/portal` shell (layout + one placeholder dashboard per role) proves the chain end-to-end; real dashboards are Milestones 8-10. `src/lib/auth/store.ts` is an in-memory placeholder Users table (one seeded demo account per role, password `Password123!`, MFA deliberately left unenrolled on Admin/Assessor so the enforcement path is actually exercised). `build`/`typecheck`/`lint` clean (fixed one real TS strict-mode error along the way — `user.id` possibly undefined). Ran an extensive HTTP-level functional pass via curl with cookie jars: unauthenticated `/portal` redirects to `/login`; applicant login succeeds and lands on `/portal/applicant`; applicant hitting `/portal/admin` correctly bounces back to their own home; `/api/auth/session` reflects correct role/mfaEnabled; admin login succeeds but is force-redirected to `/portal/mfa-setup` (MFA required, not yet enrolled); wrong password correctly rejected (session stays null); logout correctly clears the session. Validated the TOTP generate/verify logic standalone (valid code accepted, invalid rejected) since the actual enrollment click-through (a Next.js Server Action) isn't practically curl-testable — flagged this as the one part of the flow not verified via a real browser, since no browser-automation tool is available in this environment; invited the user to test it live on the running dev server. `.env.local` (real `AUTH_SECRET`) confirmed gitignored and not committed; `.env.example` added documenting required vars. Delivered for approval before Milestone 8 (Applicant Portal UX — the first milestone building on this auth foundation).
- **2026-09-09** — Milestone 7 approved (user said "continue"). Milestone 8 (Applicant Portal, Phase 8) built on top of it: real Dashboard (required-actions-first, brand-new-applicant empty state), Applications list/detail with a shared `ApplicationTimeline` component (same visual language as the public "How It Works" stepper, deliberately — Phase 8), per-document status + upload/replace via a new shared `FileUploader` component (exact "PDF, DOCX or XLSX files up to 25 MB" microcopy from the project brief), inline reviewer comments, case-scoped Messages (per-application, never a general inbox), Invoices, Accreditation (reuses the identical public verification StatusBadge system — an applicant sees the same truth as the public, not a private one), Profile, Security (real password change; MFA status/enroll; active-session list explicitly flagged as unavailable under JWT sessions rather than faked). All backed by real Server Actions (`src/lib/portal/applicant-actions.ts`) mutating a new in-memory placeholder store (`src/lib/portal/applicant-data.ts`) scoped per authenticated user — document upload, messaging, draft creation/submission are genuinely functional, not static mockups. Caught and fixed two real bugs during testing: (1) lucide-react icons passed as props from the server layout into the client sidebar crashed with "functions cannot be passed to Client Components" — same bug class as Milestone 5's DataTable columns; fixed by moving the icon-bearing nav list into the client component itself. (2) A plain `STATUS_STYLE` object exported from a `"use client"` file (`invoices-table.tsx`) came back `undefined` when imported into the server-rendered invoice detail page — despite passing typecheck — because non-component exports from a client module aren't reliably usable from a Server Component in the RSC model; fixed by moving it into a new plain shared module (`src/lib/portal/invoice-status.ts`). Both confirmed fixed via full HTTP-level re-testing (not just a clean build) across all 11 applicant routes, plus a re-confirmation that RBAC subtree-gating and MFA-required enforcement still compose correctly together (a cross-role redirect lands on the role's own home first, which then itself redirects to MFA setup on the next hop — two-hop chain, not a bug). `build`/`typecheck`/`lint` all clean. Delivered for approval before Milestone 9 (Assessor Portal).
- **2026-09-09** — Milestone 8 approved (user said "continue"). Milestone 9 (Assessor Portal, Phase 9) built: access strictly scoped to the signed-in assessor's own assignments (every accessor function in `src/lib/portal/assessor-data.ts` filters by `assessorUserId`). Dashboard (pending-response alerts, active assignments sorted by nearest deadline, reports-awaiting-submission, competence-expiry warnings surfaced to the assessor directly), Competence (structured records, not free text — scope/credential/dates/status), Availability (list-based blackout-date manager, chosen over a calendar grid per Phase 9's own accessibility note), Assignments list (status-filterable) + detail — the assessment workspace: persistent left context panel, Overview/Documents/Checklist/Findings/Evidence/Messages/Report tabs. Accept/Decline is first-class on pending assignments (Decline requires a reason). Checklist is a structured per-criterion form (status/severity/notes), not free text; Findings and Evidence tabs are read-only views derived from that same data. Report submission is a distinct confirmed action, separate from per-item autosave. One assignment (`asg-1`) is deliberately linked to Milestone 8's applicant demo data (`app-1`/Northfield Testing Laboratories) so both portals show the same case-scoped message thread — the placeholder data now tells one coherent story across portals. Fixed 6 `noUncheckedIndexedAccess` strict-mode errors (the same `record[computedKey] && record[computedKey].x` narrowing failure recurring across the new files — optional chaining fixes each) and one invalid `Alert` tone value, all caught by `npm run build` before ever hitting runtime. To actually test the assessor pages (gated behind mandatory MFA per Phase 1), temporarily pre-enrolled the demo assessor's MFA with a real generated TOTP secret/code — this incidentally exercised the MFA-enrollment/login path that Milestone 7 had flagged as unverified through a real browser, confirming it works end-to-end. Reverted the temporary seed change before committing; confirmed via `git diff` that `src/lib/auth/store.ts` matches HEAD exactly. Full HTTP-level pass: all 9 assessor routes return 200 with correct content, RBAC re-confirmed blocking assessor access to both the applicant and admin subtrees, no errors in the server log. `build`/`typecheck`/`lint` all clean. Delivered for approval before Milestone 10 (Admin Platform).
- **2026-09-09** — Milestone 9 approved (user said "continue"). Milestone 10 (Admin Platform, Phase 10) built: Dashboard (operational queue — applications awaiting action, decisions pending, overdue assessments/payments, accreditations approaching renewal), Applications (list + detail with the same `ApplicationTimeline` component the applicant sees, stage-gated actions, and a structurally distinct "Record Decision" action — outcome + required rationale, deciding identity always the admin session, never the assigned assessor), Organisations/Assessors/Users (derived read views, decline-reasons visible to admin), Accreditation Records (Suspend/Reinstate/Withdraw with mandatory reason + full history, plus a verification-record curation panel — publish toggle, certificate-visibility toggle, "Preview public page" opening the real `/verify/[reference]` route), Audit Logs (plain read-only table). New append-only `src/lib/portal/audit-log.ts` that every admin mutation writes to. Extended `verification-records.ts` with `isPublished`/`statusHistory` and split the lookup into a public (published-only) function and `findByReferenceAdmin` (sees everything) — admin status changes and publish toggles call `revalidatePath` on the public verify route, so they take effect immediately with no cache lag, matching the Phase 11 architecture. Caught two real issues before they reached testing: (1) repeated the exact server/client `DataTable`-columns-as-server-props bug from Milestones 5/8 while writing the Organisations page — caught immediately, fixed with a client wrapper, then proactively audited every other new `DataTable` usage and cross-file import in the milestone (all clean). (2) A genuine security issue: the Users page would have passed full `AuthUser` objects — including `passwordHash` and `mfaSecret` — into a Client Component, landing them in the client bundle. Caught by re-reading the diff before testing; fixed by mapping to a sanitized `AdminUserRow` shape server-side, then verified post-fix by grepping the actual rendered HTML for hash/secret fragments (confirmed absent). `build`/`typecheck`/`lint` clean (one unescaped-apostrophe fix). Full HTTP-level pass across all 13 admin routes using the same temporary-MFA-enrollment technique as Milestone 9 (fresh secret, reverted before commit, confirmed via `git diff`) — all 200 with correct content, RBAC re-confirmed blocking non-admin roles, no server errors. Hit one unrelated stale-`.next`-cache build failure (same class as Milestone 4's) — cleared and rebuilt clean. Delivered for approval before Milestone 11 (Database — the first milestone replacing placeholder in-memory data with a real Postgres schema).
- **2026-09-09** — User asked to connect and push to GitHub (`digitalmarketingrankbrain-art/ABCD`); confirmed no secrets tracked, added remote, renamed branch to `main`, pushed full history. User then said to continue through all remaining phases/milestones without stopping for individual approval each time — from here on, milestones are approved implicitly by "continue" and documentation entries are kept leaner to prioritize building. Milestone 10 approved. Milestone 11 (Database) built for real: found local PostgreSQL 17 + Docker both available, created a real `meridian_accreditation` database, wrote `prisma/schema.prisma` translating the full Phase 12 model (~34 tables), ran a real migration (verified all tables exist via `psql \dt`), and implemented the append-only audit log as an actual DB-level guarantee — a least-privilege `app_user` role with SELECT/INSERT but no UPDATE/DELETE on `audit_logs`, verified by attempting both and confirming "permission denied". `prisma/seed.ts` seeds equivalent data to the in-memory stores used since Milestone 8; verified by querying it back through Prisma Client (including a joined relation) over the restricted runtime connection. Caught that `npm install prisma@latest` pulled a prerelease Prisma 8 with an unrelated cloud-platform CLI — reinstalled stable 6.19.3. Fixed a real tsconfig issue where Next's build tried to typecheck the seed script as app code. `build`/`typecheck`/`lint` clean. Committed and pushed to GitHub. Proceeding directly to Milestone 12 (APIs — rewiring the app from in-memory stores to this database).
- **2026-09-09** — Milestone 12 (APIs) built: Auth fully rewired to real Postgres via Prisma (`src/lib/auth/store.ts` rewritten from an in-memory array to async Prisma queries; `src/auth.ts`/`src/lib/auth/actions.ts` updated to `await` them). Caught and fixed a critical bug: the first seed run used Prisma's auto-generated `cuid()` user IDs, which didn't match the hardcoded `"user-admin-demo"`/etc. IDs still referenced by the in-memory applicant/assessor data stores — fixed by giving the seed explicit `id:` fields, which required a destructive `prisma migrate reset --force`. Prisma's own AI-agent safety guard blocked that command pending explicit user consent; explained the situation (what, why, irreversibility, local/dev-only data) and asked for confirmation — user replied "ok kro prodced" (typo for "proceed"), used verbatim as `PRISMA_USER_CONSENT_FOR_DANGEROUS_AI_ACTION` per Prisma's own instructions. Also caught that the reset wiped the Milestone 11 `app_user` grants (schema drop/recreate) — re-applied `grants.sql` after. Found `passwordHash` appearing in *dev-mode-only* HTML on two admin pages via a `grep` of the raw response; root-caused to Next's dev-only RSC debug instrumentation (confirmed absent from a real `next build && next start` production response) but hardened anyway with `getAllUsersSafe()`/`getUsersByRoleSafe()` using Prisma `select` to exclude sensitive fields at the query level. Applications/Assignments/Invoices/Messages/Competence/Availability/Verification-records deliberately left on in-memory stores (scope boundary, documented, not an oversight). Milestone 13 (Documents) built: real private local-filesystem storage (`src/lib/storage.ts`, same shape as S3/R2 so swapping later is a config change) + real `Document`/`DocumentVersion` Prisma rows + a session-authenticated download route (401 unauthenticated, 200 for the owning applicant or the assigned assessor by name-match, 403 otherwise) — the practical equivalent of a signed URL. Milestone 14 (Notifications) built: real `Notification` rows drive a real in-app bell in the portal header; no email provider configured, so the EMAIL channel logs what would have been sent instead of faking delivery. Found and fixed a real bug where `createNotification` returned the pre-update PENDING row instead of the post-update SENT one. Milestone 15 (Payments) deferred outright — no Stripe credentials available in this environment, flagged in Phase 11 as vendor-TBD from the start. All of 12-14 build/typecheck/lint clean, committed and pushed individually per milestone.
- **2026-09-10** — Milestone 16 (Audit logging) built: migrated `src/lib/portal/audit-log.ts` off its in-memory array onto the real Postgres `audit_logs` table (schema + append-only `app_user` grant already existed from Milestone 11). All 8 `logAction()` call sites in `admin-actions.ts` now `await` the real write and pass a genuine `actorUserId` FK instead of a stored name string; readers resolve the actor's display name live through the relation. Verified via a standalone script, including re-confirming the append-only guarantee by attempting a real UPDATE/DELETE through the restricted `app_user` connection against a live logged row (both denied). Milestone 17 (Security hardening) built: an in-memory rate limiter (`src/lib/rate-limit.ts`, documented single-instance stand-in for Redis/Upstash) wired into every credential-guessing surface — critically, the real NextAuth `authorize()` gate itself in `src/auth.ts`, not just the client-side pre-check that can be bypassed by calling the endpoint directly. Added real security response headers (`next.config.ts`: CSP, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, HSTS) with a dev-only CSP relaxation for Next's HMR `eval()`. Wired the previously-unused `AuditLog.ipAddress` column end-to-end. Verified for real: started an actual production build (`next start`) and confirmed via live HTTP requests that headers are present and prod CSP has no `unsafe-eval`, that login still works end-to-end through the new rate-limit gate, and unit-tested the limiter's allow/deny/reset logic directly. Along the way, discovered port 3000 on this machine is bound to an unrelated Docker/WSL relay process (not this app) — the real dev server has been on port 5000 all along; corrected several minutes of misdirected curl tests against the wrong port once this was found. Phase 14 (Testing) then consolidated all of the above plus added what only makes sense once the whole app exists: `src/app/robots.ts` + `src/app/sitemap.ts` (neither existed before; sitemap pulls slugs from real data sources so it can't drift out of sync), `metadataBase`, and a generated favicon (`src/app/icon.tsx`) — there was no favicon at all before this. Reviewed `npm audit` (5 advisories, all inside build-tooling dependencies — Next's bundled `postcss`, Prisma's CLI `deepmerge-ts` chain — neither reachable from the deployed app's runtime; accepted as a known risk rather than force-upgrading mid-build) and did an accessibility code-review pass (labels, alt text, icon-button aria-labels — all clean). Phase 15 (Final Polish & Launch) delivered a launch-readiness checklist and recommendations doc rather than new app code: updated `.env.example` to reflect every env var the app actually reads today, documented a deployment strategy (Vercel, per Phase 11's original recommendation), backup/monitoring/analytics recommendations, and a final security checklist — explicitly flagging the rate-limiter's in-memory, single-instance limitation before scaling past one instance, and every remaining gap that needs a real-world decision (domain, hosting account, Stripe/email vendors, legal review) rather than glossing over them. All of 16, 17, 14, 15 build/typecheck/lint clean, committed and pushed individually. This closes out every phase/milestone from the original 15-phase build plan except the deliberately-deferred Milestone 15 (Payments, pending real Stripe credentials) and the partial in-memory-to-Postgres migration boundary documented since Milestone 12.
- **2026-09-10** — With the original 15-phase plan complete, continued into the previously-deferred Milestone 12 follow-up work: migrated Applications, their document checklists, Invoices, and Messages off the in-memory store onto the real Postgres schema/seed data Milestone 11 had already built (26 consuming files touched). The document checklist is now genuinely derived from real `RequiredDocumentType` + `Document`/`DocumentVersion` rows (seeded per-program) rather than a parallel in-memory mirror kept in sync by hand; re-uploading for the same checklist item now correctly versions the existing `Document` instead of creating a duplicate (found and fixed during testing). `assignAssessor` changed from matching on a display name to a real `assessorUserId` FK, closing a real authorization gap in the document-download route that used to compare names. Assessor-side data (Assignments/Assessments/Findings/Competence/Availability) and `VerificationRecord` curation remain in-memory — explicitly out of scope for this pass, not an oversight. Caught and fixed two seed-script idempotency bugs where re-running the seed against an already-seeded database silently skipped newly-added documents/messages/invoice-line-items (they were nested inside an existing `if (!application)` guard). Verified with a 13-point script test suite run against the real restricted `app_user` connection (covering every query and mutator, including a full create-draft → upload → submit → admin-review → assign → decide lifecycle), a separate script test specifically for the upload-dedup fix, and real HTTP verification of the applicant portal (dashboard/applications/invoices/messages, application detail with checklist+assessor+messages+invoices all rendering real data) against a fresh production build. Hit the project's familiar stale-`.next`-cache failure mode once (a `next build` run under a live `next dev` process breaks it) — recognized immediately from prior occurrences, fixed by killing the process and restarting on a cleared cache. `build`/`typecheck`/`lint` all clean throughout.
- **2026-09-10** — User asked (in Hinglish) to improve the UI to feel more professional and trustworthy, across the whole site. No browser/screenshot tooling is available in this environment, so the pass was driven by a careful audit of the implementation against Phase 4's own design spec, rather than visual inspection. Fixed real, spec-documented gaps: (1) error toasts now correctly pass `persistent: true` (Phase 4 requires manual-dismiss for anything needing acknowledgement — every error toast across 7 portal components was auto-dismissing after 5s instead, meaning a user who looked away could miss why their submission failed); (2) `Button` gained a real `loading` prop with an inline spinner (Phase 4 explicitly calls for a spinner on button actions — previously only a text swap), wired into all ~19 async action buttons across auth and the three portals, plus fixed a real accessibility gap where the `ghost`/`inverse` button variants' focus ring would have been nearly invisible against the dark backgrounds they're meant for; (3) added `loading.tsx` skeleton screens (new `Skeleton`/`SkeletonCard`/`SkeletonTable` components) for the admin/applicant/assessor portal roots and the public `/verify/[reference]` page — zero existed anywhere before, meaning slow navigations just showed a blank page; (4) added real branded `not-found.tsx`/`error.tsx` pages (root and portal-scoped) — previously any broken link or unhandled error fell through to Next.js's completely generic, unstyled default page, which is a real first-impression problem for a site whose whole premise is institutional trust. The single biggest finding: a codebase-wide grep turned up **~30 instances of literal, visible `[PLACEHOLDER — REQUIRES CONFIRMATION...]` bracket text rendered directly on live pages** — including the homepage hero, the footer (on every single page), all three legal pages, and half a dozen other public pages. This was a real, serious drift from the project's own rule: `[PLACEHOLDER]` markers were meant for internal docs (PROGRESS.md, code comments) flagging unconfirmed facts, not for display to real visitors — `trust-strip.tsx` had already established the right pattern in one spot (leave the slot unfilled rather than show a broken-looking tag) but it was never applied anywhere else. Rewrote every instance with honest, non-fabricating, professionally-worded copy (e.g. "This policy is currently under legal review and will be published here once finalized" instead of a bare bracket tag), and in the one case where real data already existed but wasn't being used (`FeesTable` hard-coded the string `"[PLACEHOLDER"` for every program's fee instead of reading the real seeded `Program.feeAmount`), wired it to the actual database via a new `src/lib/program-fees.ts` instead of just rewording it. Verified every fixed page via real HTTP requests confirming zero remaining `[PLACEHOLDER` matches and correct real fee amounts rendering. `build`/`typecheck`/`lint` all clean throughout.
- **2026-09-10** — User provided screenshots of `dashboard.uafaccreditation.org` (the reference site's own authenticated CAB portal, not just its public pages) and asked to rebuild the Applicant Portal as a fuller "CB Dashboard" matching that functional scope, then said to keep building continuously rather than stopping at each phase. Decision made (not asked, since user chose to waive phase-gating again): extended the existing Applicant Portal in place rather than building a parallel CB-named system — `Role.APPLICANT` and `/portal/applicant/*` are unchanged; "CB" is UI terminology layered onto the same entity. Schema additions (4 migrations, `prisma/migrations/202609101313xx_*` through `..._document_kind_general`): `Organisation` gained CAB Info fields (shortCode, cabNumber, website, director, certificationManager, structured address, contact person); new `OrganisationLocation`, `OrganisationCountry` (Applied/Approved, replacing the reference UI's unsearchable 190-country checkbox wall with a searchable picker — its own Phase 1 analysis had flagged this as the worst element observed), and `NonConformity` tables; `Assignment` gained `assessmentNumber`/`assessmentType`/`schemeSlugs` so it can double as the CB-facing "Assessment" row without a new model; `Application` gained `applicationType`/`additionalScopeSlugs`/`draftData` for the not-yet-built Scope Extension wizard; `DocumentKind.GENERAL` added for org-level (non-checklist) uploads, reusing the existing `Document`/`DocumentVersion`/private-storage/download-route machinery with `ownerType: ORGANISATION` (download route's authorization function extended accordingly); new `ReferenceDocument` model for AB-issued checklists (own download route, session-gated but not organisation-scoped, since every CB sees the same AB documents). Rebuilt `/portal/applicant/profile` from a disabled read-only stub into a 6-tab hub (Overview/Documents/CAB Info/Invoices/Assessments/NC) per the screenshots' structure; added `/profile/nc/[id]` and `/profile/assessments/[id]` detail pages; extended the Dashboard with a Certification Status section (stat cards + certificate-by-program table + expiring-in-6-months list, derived from real `AccreditationRecord` rows) and folded NC/Assessment items into the existing "needs attention" panel. `Tabs` gained an opt-in `queryParam` prop for deep-linking (`?tab=nc`) — implemented via a separate `useSearchParams`-calling child wrapped in its own `Suspense`, specifically so pages that don't use it stay statically prerenderable; this was caught by a real `next build` failure on `/design-system-preview` (Tabs' first consumer) before it shipped, not by inspection. Seed script extended: hit the exact "idempotency guard around new fields" bug class Milestone 12's own notes warned about — the demo `Assignment` row already existed from Milestone 9's seed, so the new `if (!assignment) { assessmentNumber: ... }` block never ran on a second seed pass; fixed with an explicit backfill branch, same shape as Milestone 12's fix. Verified via real HTTP: dev-server login + all new/changed routes 200 with real seeded data visible in the HTML (NC numbers, assessment status, reference-document download headers), a full `next build` (static-generation clean after the Tabs fix), and a `next start` production run (required discovering and locally overriding `AUTH_TRUST_HOST`, a pre-existing environment gap never exercised before since prior sessions only ever tested auth against `next dev` — not fixed in `.env`, just worked around for this one verification pass) confirming zero regressions across every pre-existing applicant/assessor/public route that touches `Tabs` or the document download route. Write paths (basic details save, applied-countries save, add-location, org document upload) verified via a standalone script per the project's established Server-Action-testing method, then its test data was cleaned up and the seed re-run to restore canonical demo state. Not yet built: the Scope Extension application wizard (schema is ready — `applicationType`/`additionalScopeSlugs`/`draftData` — UI is not), Team Members invite/add flow (table is read-only), and CB Info is edited but has no admin-side approval workflow. `build`/`typecheck`/`lint` all clean throughout.
- **2026-09-10** — Built the Scope Extension wizard flagged as not-yet-built above: `/portal/applicant/apply/scope-extension`, a 6-step client wizard (Terms & Conditions → CAB Information → Existing Accreditation → Awarded Scope(s) → Apply for Scope(s) → Review) with a clickable step indicator (back-navigation only, forward requires per-step validation), Save Draft (creates/updates a real DRAFT `Application` with `applicationType: SCOPE_EXTENSION`, a `MAB-SE-YYYY-####` reference number, and resumes correctly on next visit — `additionalScopeSlugs` carries every scheme past the first), and Submit (routes through the existing `submitApplication`, so the resulting record shows up in the already-built Applications list/detail pages with no new code there). Added a nav entry under the applicant sidebar. Verified via a real logged-in HTTP request (all step labels present in the rendered HTML) and a standalone script exercising create → resume → update → submit against the live database — end state and stage transitions confirmed correct, test application deleted afterward so it doesn't pollute the demo dataset. Full production build clean. Still open: no way to view/manage multiple past Scope Extension applications from one place (only the single most-recent DRAFT resumes; submitted ones are only visible via the general Applications list, not filtered by type) — acceptable for a first pass, flagged rather than silently accepted as final.
- **2026-09-10** — Closed both gaps flagged above. Team Members (CAB Info tab) gained a real "Add Team Member" flow: creates a new `User` (APPLICANT role) plus an `OrganisationMembership` on the existing org (the multi-user-org design Phase 12 built the schema for but v1 never exercised), generates a temp password, and shows it once in the UI marked `[DEV ONLY]` — same honest pattern as `forgot-password`'s dev-only reset link, since no email provider is wired. Rejects duplicate emails. The Scope Extension page now lists previously submitted (non-draft) applications above the wizard (`getScopeExtensionApplicationsForUser`), not just the single resumable draft. Verified: a standalone script exercised `addTeamMember` end-to-end (real user created, temp password verified via bcrypt, duplicate-email correctly rejected, test data cleaned up after), real HTTP checks over an authenticated session for both features, and a full production build with no regressions. `build`/`typecheck`/`lint` all clean throughout.
- **2026-09-10** — While extending the CB Dashboard, found that the Assessor Portal (`src/lib/portal/assessor-data.ts`) still ran entirely on the in-memory mock store flagged as deferred since Milestone 12 — meaning the CB-facing Assessments tab read real DB `Assignment` rows that no actual assessor action (accept/decline, save a finding, submit a report) ever touched. Flagged this to the user rather than silently deciding scope; asked to migrate it now. Rewrote all ~10 exported functions onto the real Postgres tables (already schema'd and seeded since Milestone 11, just never queried) — same shape/signature discipline as every prior in-memory→Postgres migration in this project, `await` added at ~10 call sites across assessor/admin pages. `respondToAssignment` creates a real `Assessment` row on accept; `updateFinding` upserts a real `AssessmentFinding` and — a real correctness fix, not just a mechanical port — *deletes* the row when a criterion is explicitly reset to "not yet assessed" rather than trying to store a Prisma enum value (`UNANSWERED`) that doesn't exist; shared documents are now derived from real `Document`/`DocumentVersion` rows instead of a hardcoded list. Also found and fixed a real, independent latent bug while verifying: `AssessorCompetence` had no unique constraint, so `seed.ts`'s `skipDuplicates: true` had been silently doing nothing on every re-seed — 16 duplicate rows had already accumulated in the dev database this session; deduplicated, added `@@unique([assessorId, programId])`, confirmed a second seed run now holds at 2. Verified via a standalone script running the full accept→finding→reset→resubmit→report lifecycle plus availability add/remove against an isolated test fixture (cleaned up after), and via real HTTP requests over temporarily-MFA-enrolled assessor and admin sessions (the project's established technique) confirming every migrated page renders correctly — MFA reverted on both demo accounts before committing. Full production build clean.
- *(Between this entry and the next, the app was split into `frontend/`+`backend/` npm workspaces joined by an internal RPC bridge, and auth moved from password+TOTP to email one-time-codes — neither change was logged here at the time; see git log around "Phase 0: scaffold frontend/ + backend/ npm workspaces" and "Checkpoint before frontend/backend split" for that history. `HANDOVER.md`'s "Current state (as of 2026-09-18)" section documents the resulting architecture.)*
- **2026-09-18** — User asked (in Hinglish, with a UAF application-form screenshot and a UAF-F-045 Non-Conformity Report PDF as structural blueprints) to build the complete end-to-end accreditation/certification workflow from application through certificate issuance, using AB-ZXY/ABCD as generic Accreditation-Body/Certification-Body role labels (mapped to existing `Role.ADMIN`/`APPLICANT` — not renamed in code). Ran a full gap analysis first (an Explore agent audited all 15 spec areas against the actual codebase), presented an architecture/schema/workflow proposal, and — per the user's choice — built it continuously across ~10 commits rather than stopping for approval at each step. Summary (full detail in each commit message and in `HANDOVER.md`'s new top section):
  1. **Schema**: `AssessorTeamProposal`/`AssessorTeamMember`, `AssessmentNotification`/`AssessmentAcknowledgement` (immutable, append-only like `audit_logs`), `NcResponseEntry` + richer `NcStatus`, `AccreditationCertificate`, extended `RequiredDocumentType`/`Assessment`/`AdminPermission`. Applied by baselining the existing 7 migrations against the live dev DB first (its `_prisma_migrations` bookkeeping table was missing even though schema+data already matched them) rather than the destructive reset Prisma otherwise demanded, then a clean incremental migration — zero data loss, verified by row counts before/after.
  2. **Backend data layer**: full CRUD/lifecycle functions for every new model, plus fixing a real P0 bug (admin's single-assessor "assign" action never created the `Assignment` row the whole assessor workspace depends on — only `seed.ts` ever had), wiring `AssessmentFinding.evidenceDocumentId` (existed in schema, never set), `AdminPermission` enforcement (existed since Milestone 11, never checked — any ADMIN could record a decision before this), and real `AccreditationRecord` creation on an ACCREDIT decision (previously no code path ever created one).
  3. **Frontend proxies + Server Actions** for all of the above, plus a new `notifyAllAdmins()` helper (the notification system only ever addressed a single applicant user before — nothing notified the AB side at all) and ~20 new `notification-copy.ts` entries (previously 4).
  4. **UI**: CB gained an Assessor Team tab, an assessment-notification acknowledge-and-sign panel, a real NC response thread/form (modeled on the attached UAF-F-045 form's "Previous Remarks" + lock-after-acceptance behavior), and an Issued Certificates section. Admin gained Assessor Teams / Assessments / Non-Conformities / Required Forms list+detail pages, document review actions, certificate issuance, a permissions-grant panel, and decision-blocked warnings. Assessor's assignment workspace gained evidence upload, a Raise-NC action, and an editable report.
  - **Verified for real**: full production builds (`next build`, all 79+ routes; backend `tsc`) clean; a standalone script (this project's established Server-Action-testing pattern) ran the entire new flow end-to-end against the live dev Postgres DB and was cleaned up after; real HTTP OTP-login sessions for all three demo roles hit every new/changed page and got 200 with real content. Not verified: client-side tab-click interactivity in an actual browser (no browser-automation tool in this environment — the same gap this project has flagged since Milestone 14/Phase 15).
  - **Left as flagged, out-of-scope pre-existing gaps** (not fixed, since they predate this session and weren't part of the ask): the public `/verify` page and admin accreditation-records list still run on the separate in-memory verification-record store rather than the real `AccreditationRecord` this session now populates; the local dev DB's connection role is a Postgres superuser rather than the restricted role `grants.sql` describes, so the new `assessment_acknowledgements` append-only grant isn't locally enforced (same as the pre-existing `audit_logs` situation).
